% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arcstat.R
\name{eq_readings}
\alias{eq_readings}
\alias{eq_readings_vsl}
\title{Quantile-domain induction readings and equivalence discrepancy for tilted beta-kernel
quantile densities q(u) = u^alpha (1-u)^beta exp(sum theta_j P_j(u))}
\usage{
eq_readings(alpha, beta, theta = numeric(0), ngrid = 20001L)

eq_readings_vsl(lambda, delta, ngrid = 20001L)
}
\arguments{
\item{alpha, beta}{kernel exponents}

\item{theta}{tilt coefficients on shifted Legendre polynomials (orders 1 to 3)}

\item{ngrid}{evaluation grid size}

\item{lambda, delta}{van Staden-Loots parameters (kurtosis and skew weights)}
}
\value{
c(D, a, b, u_c, u_b); NA when the geometry is invalid
}
\description{
Quantile-domain induction readings and equivalence discrepancy for tilted beta-kernel
quantile densities q(u) = u^alpha (1-u)^beta exp(sum theta_j P_j(u))
}

# ellstat (Python)

The elliptic moment system for circular data. ctypes front to the same C back end
that backs the R package `ellstat`; values are bit-identical across the two fronts,
which is checked by `parity_ellstat.sh`.
